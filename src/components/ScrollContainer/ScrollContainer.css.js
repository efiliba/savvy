import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 400,
    padding: 15,
    borderWidth: 5,
    backgroundColor: 'white',
    borderRadius: 10
  }
});
